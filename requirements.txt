# =========================================================
# RouteTREE - Phase 1 (Road Recovery Comparison)
# requirements.txt
# =========================================================
# Install with:
#   pip install -r requirements.txt
#
# NOTE ON GPU: this installs the CPU build of torch by default
# via PyPI. If you have an NVIDIA GPU, install torch separately
# FIRST using the command generator at https://pytorch.org/get-started/locally/
# (pick your CUDA version), THEN run this file -- pip will see
# torch is already installed and skip it.
# =========================================================

# ---- Core deep learning ----
torch>=2.1.0
torchvision>=0.16.0

# ---- Segmentation model library (provides DeepLabV3+, U-Net, encoders) ----
segmentation-models-pytorch>=0.3.3

# ---- Image augmentation ----
albumentations>=1.4.0

# ---- Image I/O and array processing ----
numpy>=1.24.0
opencv-python>=4.9.0
Pillow>=10.0.0

# ---- Metrics / evaluation ----
scikit-learn>=1.3.0

# ---- Progress bars ----
tqdm>=4.66.0

# ---- Plotting (used in visualize_results.py) ----
matplotlib>=3.8.0

# ---- Data handling ----
pandas>=2.1.0

# ---- Saving metrics in tabular form ----
openpyxl>=3.1.0

# =========================================================
# OPTIONAL - only needed for the one-time vector-to-mask
# conversion step (prepare_vector_masks.py) for SpaceNet
# and OpenSatMap. NOT required for training/eval/predict.
# Uncomment if you need to run that script:
# =========================================================
# rasterio>=1.3.9
# geopandas>=0.14.0
# shapely>=2.0.0